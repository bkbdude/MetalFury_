using UnityEngine;
using System.Collections;

public class SpawnPoint : MonoBehaviour {

    public bool matchStartSpawn;
    public int playerId = 0;// if -1 Then all players can use the point

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
